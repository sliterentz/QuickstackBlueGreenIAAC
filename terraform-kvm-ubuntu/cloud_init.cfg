#cloud-config
# Konfigurasi Cloud-Init Optimal & Production-Ready untuk Kubernetes

hostname: ${hostname}
fqdn: ${hostname}.localdomain
manage_etc_hosts: true

# 1. Update & Upgrade
package_update: true
package_upgrade: false

# 2. Essential Packages
packages:
  - qemu-guest-agent
  - net-tools
  - curl
  - wget
  - apt-transport-https
  - ca-certificates
  - gnupg
  - lsb-release
  - ufw
  - socat
  - conntrack
  - ipset
  - nfs-common
  - open-iscsi

# 3. Create Files (Hardening & Config)
write_files:
  # Kernel Modules for K8s
  - path: /etc/modules-load.d/k8s.conf
    content: |
      overlay
      br_netfilter

  # Sysctl for K8s
  - path: /etc/sysctl.d/k8s.conf
    content: |
      net.bridge.bridge-nf-call-iptables  = 1
      net.bridge.bridge-nf-call-ip6tables = 1
      net.ipv4.ip_forward                 = 1
      vm.overcommit_memory                = 1
      vm.panic_on_oom                     = 0
      kernel.panic                        = 10
      kernel.panic_on_oops                = 1
      kernel.keys.root_maxkeys            = 1000000
      kernel.keys.root_maxbytes           = 25000000

  # Containerd Config
  - path: /etc/containerd/config.toml
    content: |
      version = 2
      [plugins."io.containerd.grpc.v1.cri".containerd.runtimes.runc]
        [plugins."io.containerd.grpc.v1.cri".containerd.runtimes.runc.options]
          SystemdCgroup = true

  # Basic CIS Hardening: Secure SSH
  - path: /etc/ssh/sshd_config.d/99-hardening.conf
    content: |
      PermitRootLogin no
      PasswordAuthentication no
      X11Forwarding no
      MaxAuthTries 3
      AllowTcpForwarding yes

# 4. User Configuration
users:
  - name: ${ssh_user}
    sudo: ALL=(ALL) NOPASSWD:ALL
    groups: users, admin, sudo
    home: /home/${ssh_user}
    shell: /bin/bash
    ssh-authorized-keys:
      - ${ssh_key}
    lock_passwd: false

# 5. Provisioning Script
runcmd:
  # Disable Swap (Requirement for K8s)
  - swapoff -a
  - sed -i '/ swap / s/^\(.*\)$/#\1/g' /etc/fstab

  # Apply Kernel Modules & Sysctl
  - modprobe overlay
  - modprobe br_netfilter
  - sysctl --system

  # Install K3s (Lightweight Kubernetes)
  # Menggunakan INSTALL_K3S_SKIP_ENABLE=true agar tidak bentrok dengan systemd saat boot awal jika diperlukan
  - curl -sfL https://get.k3s.io | INSTALL_K3S_EXEC="--write-kubeconfig-mode 644 --tls-san ${hostname} --node-name ${hostname}" sh -

  # Setup Firewall (UFW) - Disabled for initial debugging
  - ufw disable
  # - ufw default deny incoming
  # - ufw default allow outgoing
  # - ufw allow ssh
  # - ufw allow 6443/tcp # K3s API Server
  # - ufw allow 10250/tcp # Kubelet API
  # - ufw allow 30000:32767/tcp # NodePort Services
  # - ufw --force enable

  # Basic Monitoring: Install Prometheus Node Exporter
  - wget -q https://github.com/prometheus/node_exporter/releases/download/v1.8.2/node_exporter-1.8.2.linux-amd64.tar.gz
  - tar xf node_exporter-1.8.2.linux-amd64.tar.gz
  - mv node_exporter-1.8.2.linux-amd64/node_exporter /usr/local/bin/
  - useradd -rs /bin/false node_exporter || true
  - |
    cat <<EOF > /etc/systemd/system/node_exporter.service
    [Unit]
    Description=Node Exporter
    After=network.target

    [Service]
    User=node_exporter
    Group=node_exporter
    Type=simple
    ExecStart=/usr/local/bin/node_exporter

    [Install]
    WantedBy=multi-user.target
    EOF
  - systemctl daemon-reload
  - systemctl enable --now node_exporter
  - ufw allow 9100/tcp # Node Exporter port

  # Cleanup & Status Signal
  - systemctl enable --now qemu-guest-agent
  - echo "DONE" > /var/lib/cloud/instance/boot-finished-k3s
  - echo "Production-Ready Ubuntu for K3s with CIS Hardening & Monitoring is ready." > /tmp/provisioning-status.txt
  - date >> /tmp/provisioning-status.txt
